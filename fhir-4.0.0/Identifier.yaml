attrs:
  assigner: {type: Reference, desc: Organization that issued id (may be just text)}
  id: {type: string, desc: Unique id for inter-element referencing}
  period: {type: Period, desc: Time period when id is/was valid for use}
  system: {type: uri, desc: The namespace for the identifier value}
  type: {type: CodeableConcept, desc: Description of identifier}
  use: {type: code, desc: usual | official | temp | secondary | old (If known)}
  value: {type: string, desc: The value that is unique}
